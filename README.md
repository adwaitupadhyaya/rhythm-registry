# rhythm-registry
This project implements an **Artist Management System** with a minimal admin panel to manage `User`, `Artist`, and `Song` records. The app implements role-based access control with three roles: `super_admin`, `artist_manager`, and `artist`.
